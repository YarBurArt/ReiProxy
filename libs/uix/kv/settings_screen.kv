<SettingsScreen>

    BoxLayout:
        orientation: "vertical"

        MDLabel:
            text: "Current Screen: " + root.name

        MDButton:
            text: "GOTO HOME"
            on_release: root.manager.pop()
