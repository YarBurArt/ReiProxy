<HomeScreen>

    BoxLayout:
        orientation: "vertical"

        MDLabel:
            text: "Current Screen: " + root.name

        MDButton:
            text: "GOTO SETTINGS"
            on_release: root.manager.push("settings")
